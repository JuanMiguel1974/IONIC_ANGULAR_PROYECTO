import { SupermercadoPipe } from './supermercado.pipe';

describe('SupermercadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SupermercadoPipe();
    expect(pipe).toBeTruthy();
  });
});
