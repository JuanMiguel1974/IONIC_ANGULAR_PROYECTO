import { EmailDirective } from './email-directive';

describe('EmailDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDirective();
    expect(directive).toBeTruthy();
  });
});
