import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juanmi.app',
  appName: 'Lista de la compra',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
